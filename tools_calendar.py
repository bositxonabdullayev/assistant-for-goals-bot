# Google Calendar integration — skeleton with TODOs
from typing import List, Dict


def list_upcoming_events(user_id: int) -> List[Dict]:
    # TODO: Use googleapiclient to list events with stored credentials
    return []


def enable_watch(user_id: int) -> Dict:
    # TODO: Create a watch channel and store resourceId
    return {"ok": False, "msg": "Not implemented yet"}
